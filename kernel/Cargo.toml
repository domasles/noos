[package]
name = "noos-kernel"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]
harness = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
spin = "0.10.0"

[dependencies.lazy_static]
version = "1.5.0"
features = ["spin_no_std"]
